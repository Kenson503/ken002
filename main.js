// Make sure Typed.js is loaded before this script
// If using a module system, uncomment the next line:
// import Typed from 'typed.js';

var typed = new Typed(".text", {
    strings: ["kenson","Ndichu"],
    typeSpeed: 50,
    backSpeed: 50,
    backDelay: 1500,
    loop: true
});